{% extends 'admin_base.html.twig' %}

{% block title %}Edit formation{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4 text-center">Modifier la formation</h1>

        <!-- Affichage des messages de succès ou d'erreur -->
        {% if app.flashes('success') %}
            <div class="alert alert-success" role="alert">
                {{ app.flashes('success')|first }}
            </div>
        {% elseif app.flashes('error') %}
            <div class="alert alert-danger" role="alert">
                {{ app.flashes('error')|first }}
            </div>
        {% endif %}

        <!-- Formulaire -->
        <div class="card p-4 shadow-sm">
            {{ include('formation/_form.html.twig', {'button_label': ''}) }}
        </div>

        <!-- Retour à la liste des formation -->
        {# <div class="text-center mt-4">
            <a href="{{ path('app_formation_index') }}" class="btn btn-secondary">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-arrow-left" viewBox="0 0 16 16">
  <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8"/>
</svg>
            Retour à la liste</a>
        </div> #}

        <!-- Formulaire de suppression -->
        <div class="text-center mt-3">
            {{ include('formation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

